export const select = query => document.querySelector(query);
export const selectAll = query => document.querySelectorAll(query);
export const toggle = (element,style) => element.classList.toggle(style);


